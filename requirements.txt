# ─────────────────────────────────────────────────────────────────────────────
# Core-Auth System — Python Dependencies
# Install with: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Core Framework ────────────────────────────────────────────────────────────
fastapi[standard]==0.115.12     # FastAPI with standard extras (email-validator, etc.)
uvicorn[standard]==0.35.0       # ASGI server with WebSocket + HTTP/2 support

# ── Database — Async SQLAlchemy ───────────────────────────────────────────────
sqlalchemy==2.0.43              # ORM + async engine (create_async_engine)
aiosqlite==0.20.0               # Async SQLite driver (local dev + tests)
asyncpg==0.30.0                 # Async PostgreSQL driver (CI + production)
alembic==1.16.1                 # Database migration tool

# ── Validation & Settings ─────────────────────────────────────────────────────
pydantic==2.11.7                # Data validation and serialization
pydantic-settings==2.10.1       # Load settings from .env file
email-validator==2.2.0          # Required by pydantic for EmailStr validation

# ── Authentication & Cryptography ─────────────────────────────────────────────
python-jose[cryptography]==3.3.0 # JWT encoding/decoding with HS256 support
passlib[bcrypt]==1.7.4           # Password hashing (bcrypt context)
bcrypt==4.2.1                    # bcrypt backend for passlib
python-multipart==0.0.20         # Required by FastAPI for form data (OAuth2 forms)

# ── Redis (In-Memory Blacklist Layer) ─────────────────────────────────────────
redis[asyncio]==5.2.1            # Async Redis client (redis.asyncio namespace)

# ── Async HTTP Client (for tests) ────────────────────────────────────────────
httpx==0.28.1                    # Async HTTP client — used with ASGITransport in tests
anyio[asyncio]==4.9.0            # Async backend — required by httpx + pytest-asyncio

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.4.1                    # Test runner
pytest-asyncio==0.24.0           # Async test support (asyncio_mode = auto)
pytest-cov==6.2.1                # Coverage reporting (--cov flag)

# ── Linting & Formatting ──────────────────────────────────────────────────────
ruff==0.9.10                     # Fast all-in-one linter (replaces flake8+isort+more)
flake8==7.3.0                    # PEP8 style checker (required by Spec §5 pipeline)
black==25.1.0                    # Code formatter

# ── Static Security Analysis (SAST) ──────────────────────────────────────────
bandit==1.8.3                    # SAST scanner (Spec §5 — detects weak crypto, hardcoded secrets)

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv==1.1.1             # Load .env file (used by pydantic-settings)