[pytest]
# Run all async test functions automatically on the asyncio event loop.
# "auto" mode means no @pytest.mark.asyncio decorator needed on each test.
asyncio_mode = auto

# Always show test names verbosely + short traceback for fast debugging
addopts = -v --tb=short

# Test discovery: look inside the tests/ directory
testpaths = tests
